default:
  target: dev
  outputs:
    dev:
      type: athena
      s3_staging_dir: "s3://{{ env_var('DBT_ATHENA_S3_STAGING', 'ons-dev-dg-00-stage') }}/athena-results/dev/"
      region_name: us-east-1
      database: analytics_dev          # ← SEM catalog_name!
      schema: dbt_dev
      work_group: primary
      threads: 4
      aws_profile_name: null
      
    prod:
      type: athena
      s3_staging_dir: "s3://{{ env_var('DBT_ATHENA_S3_STAGING', 'ons-dev-dg-00-stage') }}/athena-results/prod/"
      region_name: us-east-1
      database: analytics_prod         # ← SEM catalog_name!
      schema: dbt_prod
      work_group: primary
      threads: 8
      aws_profile_name: null
      poll_interval: 1.0
      num_retries: 3